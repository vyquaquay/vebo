{% extends 'base.html.twig' %}

{% block title %}Club{% endblock %}

{% block body %}
    <h1>Club</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ club.id }}</td>
            </tr>
            <tr>
                <th>Club_ID</th>
                <td>{{ club.ClubID }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ club.Name }}</td>
            </tr>
            <tr>
                <th>Player</th>
                <td>{{ club.Player }}</td>
            </tr>
            <tr>
                <th>Coach</th>
                <td>{{ club.Coach }}</td>
            </tr>
            <tr>
                <th>Host_Stadium</th>
                <td>{{ club.HostStadium }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_club_index') }}">back to list</a>

    <a href="{{ path('app_club_edit', {'id': club.id}) }}">edit</a>

    {{ include('club/_delete_form.html.twig') }}
{% endblock %}
